const {autores} = require("./autores");

function excluirAutor(nome) {
 autores.forEach(autor =>(
    l
 ))
}

const dados = [1,2,3,4,5,6,0];

const index = dados.indexOf(4);

const element = dados.splice(index, 1); 

